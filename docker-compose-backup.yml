version: '1.0'
services:
  solarmon-frontend:
    build: 
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
    environment:
      - REACT_APP_NAME=solarmonfrontend
      - CHOKIDAR_USEPOLLING=true